---
title: Hello, world
description: A first placeholder post. Template for future writing — delete or replace.
pubDate: 2026-04-23
draft: true
tags: [meta]
---

This is a template post. It is marked `draft: true` so it does not appear in listings.

Copy this file to start a new post, then:

1. Rename the file to a URL-friendly slug (e.g. `building-gothired.md`)
2. Update the frontmatter: `title`, `description`, `pubDate`, `tags`
3. Flip `draft: true` to `draft: false` when ready to publish
4. Commit — Cloudflare Pages builds on push
